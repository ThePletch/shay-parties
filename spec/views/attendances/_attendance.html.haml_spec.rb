require "rails_helper"

RSpec.describe "attendances/attendance" do
  it "displays a gravatar for the user"
  it "shows if the attendee is a guest"
  it "shows if the attendee is a host"
  it "shows a delete button to the host"
  it "shows emails to the host"
  it "hides emails from guests"
end
